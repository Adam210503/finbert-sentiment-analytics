# sentiment-pipeline — backend dependencies
# Pin versions for reproducibility after running:
#   pip freeze > requirements.txt

# Data collection
requests>=2.31.0
yfinance>=0.2.38
newsapi-python>=0.2.7     # optional convenience wrapper (unused; kept for reference)

# Scheduling
APScheduler>=3.10.4

# ML / inference (Phase 2 — not needed for Phase 1 data collection)
transformers>=4.40.0
torch>=2.2.0

# Data processing
pandas>=2.0.0
numpy>=1.26.0

# Dashboard
streamlit>=1.35.0
plotly>=5.20.0

# Utilities
python-dateutil>=2.9.0
pytz>=2024.1
